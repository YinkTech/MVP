<%= form_with(model: external) do |form| %>
  <% if external.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(external.errors.count, "error") %> prohibited this external from being saved:</h2>

      <ul>
        <% external.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :company_name, style: "display: block" %>
    <%= form.text_area :company_name %>
  </div>

  <div>
    <%= form.label :products_name, style: "display: block" %>
    <%= form.text_field :products_name %>
  </div>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.text_field :quantity %>
  </div>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.text_field :amount %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
